# Apps in Toss Unity Plugin - 사용자 가이드

**버전**: 1.0.1
**최종 업데이트**: 2025-10-21

---

## 📋 목차

1. [개요](#개요)
2. [시작하기](#시작하기)
3. [사용 방법](#사용-방법)
4. [문제 해결](#문제-해결)
5. [FAQ](#faq)

---

## 개요

Apps in Toss Unity Plugin은 Unity 게임을 Apps in Toss 미니앱 플랫폼에 쉽게 배포할 수 있도록 도와주는 Unity Editor 확장 도구입니다.

### 주요 기능

- ✅ **Settings 통합**: 설정과 빌드가 하나의 창에서
- ✅ **원클릭 빌드**: WebGL 빌드부터 패키징까지 한 번에
- ✅ **AITTemplate**: Apps in Toss 최적화된 WebGL 템플릿
- ✅ **개발 서버**: 로컬에서 실시간 테스트 가능 (Python HTTP Server)
- ✅ **직관적 UI**: Unity Editor 내 편리한 인터페이스
- ✅ **실시간 로그**: 빌드 진행 상황을 실시간으로 확인

### 지원 환경

- **Unity 버전**: 2021.3 이상
- **플랫폼**: macOS, Windows
- **필수 소프트웨어**: Python 3 (개발 서버용, 선택사항)

---

## 시작하기

### 1. SDK 설치

Unity 프로젝트에 Apps in Toss SDK를 추가합니다.

#### 방법 1: Package Manager (로컬 패키지)

1. Unity Editor 열기
2. `Window > Package Manager` 메뉴 클릭
3. 좌측 상단 `+` 버튼 → `Add package from disk...` 선택
4. SDK 폴더의 `package.json` 파일 선택
   ```
   apps-in-toss-unity-transform-sdk/package.json
   ```

#### 방법 2: manifest.json 직접 수정

`Packages/manifest.json` 파일에 다음 라인 추가:

```json
{
  "dependencies": {
    "com.appsintoss.miniapp": "file:../../../apps-in-toss-unity-transform-sdk"
  }
}
```

### 2. Python 확인 (선택사항 - 개발 서버용)

터미널에서 Python이 설치되어 있는지 확인:

```bash
python3 --version
```

대부분의 macOS/Linux에는 기본적으로 설치되어 있습니다.

### 3. Unity Editor 재시작

SDK가 제대로 로드되도록 Unity Editor를 재시작합니다.

---

## 사용 방법

### 1. Build & Deploy Window 열기

Unity Editor 상단 메뉴:
```
Apps in Toss > Build & Deploy Window
```

### 2. Settings 입력

창 상단의 **"⚙️ 설정"** 섹션을 펼쳐서 다음 정보를 입력합니다:

#### 앱 기본 정보
- **앱 이름**: 게임 이름 (예: "Flappy Bird")
- **버전**: 앱 버전 (예: "1.0.0")
- **설명**: 앱 설명

**참고**: 앱 ID는 Unity 프로젝트 이름(PlayerSettings.productName)이 자동으로 사용됩니다.

#### 브랜드 설정
- **기본 색상**: 앱의 기본 테마 색상 (예: "#3182F6")
- **아이콘 URL**: 앱 아이콘 이미지 URL

#### 개발 서버 설정
- **로컬 포트**: 개발 서버 포트 번호 (기본값: 5173)

#### 빌드 설정
- **프로덕션 모드**: 프로덕션 환경으로 배포할 때 체크
- **최적화 활성화**: 빌드 최적화 옵션

#### 광고 설정 (선택사항)
- **광고 활성화**: 광고 기능 사용 시 체크
- **광고 ID**: 각종 광고 ID 입력

**Tip**: Settings 섹션은 접었다 펼 수 있습니다. 화살표를 클릭하세요.

### 3. Build & Package 실행

```
🚀 Build & Package 버튼 클릭
```

이 버튼 하나로 다음 작업이 자동 실행됩니다:

1. **Unity WebGL Template 설정** (AITTemplate2022 자동 적용)
2. **WebGL 빌드** (`webgl/` 폴더에 생성)
3. **빌드 복사** (`ait-build/` 폴더로 복사)

**예상 소요 시간**: 3-10분 (프로젝트 크기에 따라)

**참고**: AITTemplate은 Apps in Toss 플랫폼에 최적화된 HTML/JavaScript를 자동으로 포함합니다.

### 4. 빌드 결과 확인

빌드가 완료되면 `ait-build/` 폴더에 WebGL 빌드가 생성됩니다.

```
📂 Open Build Folder 버튼 클릭
```

로컬에서 테스트하거나, 폴더를 압축하여 Apps in Toss 콘솔에 업로드하세요.

---

## 빌드 옵션

### 🔨 WebGL Build Only

WebGL 빌드만 실행합니다. 패키징은 하지 않습니다.

**사용 시나리오**: 빌드만 다시 하고 싶을 때

### 📦 Package Only

WebGL 빌드 결과물을 사용하여 패키징만 실행합니다.

**사용 시나리오**:
- WebGL 빌드는 이미 완료된 상태
- app.json 설정만 변경하고 싶을 때

**전제 조건**: WebGL 빌드가 완료되어 있어야 함

### 🚀 Build & Package

WebGL 빌드 + 패키징을 한 번에 실행합니다.

**사용 시나리오**: 처음 사용하거나 전체 프로세스가 필요할 때

### ▶️ Start Dev Server

개발 서버를 시작합니다. 로컬에서 미니앱을 실시간으로 테스트할 수 있습니다.

**사용 시나리오**:
- 빌드 후 로컬에서 테스트할 때
- 개발 중 빠른 반복 테스트가 필요할 때

**주소**: `http://localhost:5173/index.html` (설정한 포트)

**기술**: Python SimpleHTTPServer (별도 설치 불필요)

### ⏹️ Stop Dev Server

실행 중인 개발 서버를 중지합니다.

### 📂 Open Build Folder

빌드 결과물이 있는 폴더를 엽니다.

**사용 시나리오**: `.ait` 파일을 찾을 때

---

## 빌드 프로세스 상세

### 1단계: Unity PlayerSettings 자동 설정

```
Unity Player Settings 자동 설정:
- WebGL Template: PROJECT:AITTemplate2022
- Compression Format: Disabled
- Threads Support: false
- Run In Background: false
- Linker Target: Wasm
- Data Caching: false
```

AITTemplate2022는 Apps in Toss 플랫폼에 최적화된 WebGL 템플릿입니다.

### 2단계: WebGL 빌드

Unity가 AITTemplate을 사용하여 `webgl/` 폴더에 WebGL 빌드를 생성합니다.

AITTemplate이 자동으로 포함하는 기능:
- **appsintoss-unity-bridge.js**: Unity와 Apps in Toss 플랫폼 간의 브릿지
- **로그인, 결제, 광고, 저장소 등 모든 API 지원**
- **로딩 화면, 토스트, 다이얼로그 UI**
- **개발/프로덕션 환경 자동 감지**

### 3단계: 빌드 복사

```
webgl/ (전체) → ait-build/
```

WebGL 빌드 전체를 `ait-build/` 폴더로 복사합니다.

**폴더 구조**:
```
ait-build/
  ├── Build/          # Unity WebGL 빌드 파일
  │   ├── *.loader.js
  │   ├── *.data
  │   ├── *.framework.js
  │   └── *.wasm
  ├── TemplateData/   # 템플릿 리소스
  └── index.html      # AITTemplate HTML (Apps in Toss 브릿지 포함)
```

### 완료!

빌드가 완료되면 `ait-build/` 폴더가 바로 배포 가능한 상태입니다.

---

## 문제 해결


### ❌ "WebGL 빌드 실패"

**원인**: Unity 설정 또는 코드 에러

**해결**:
1. Unity Console 확인 (Window > General > Console)
2. 에러 메시지 확인
3. WebGL Build Support가 설치되어 있는지 확인:
   - Unity Hub > Installs > Add Modules > WebGL Build Support

---

### ❌ "개발 서버 시작 실패"

**원인**: Python이 설치되지 않았거나 포트가 이미 사용 중

**해결 방법 1 - Python 확인**:
```bash
python3 --version
```

**해결 방법 2 - 포트 변경**:
Settings에서 "로컬 포트"를 다른 번호로 변경 (예: 8080, 8000 등)

**해결 방법 3 - 수동으로 서버 실행**:
```bash
cd ait-build
python3 -m http.server 5173
```

---


## FAQ

### Q1: 빌드 시간이 너무 오래 걸립니다

**A**: 첫 빌드는 오래 걸릴 수 있습니다. 다음 방법으로 최적화할 수 있습니다:
- 불필요한 에셋 제거
- Build Settings에서 Scenes in Build 최소화
- Development Build 옵션 사용 (테스트용)

---

### Q2: .ait 파일은 어디에 생성되나요?

**A**: `ait-build/dist/` 폴더에 생성됩니다. "📂 Open Build Folder" 버튼으로 찾을 수 있습니다.

---

### Q3: 기존 AITEditorWin 메뉴는 어떻게 되나요?

**A**: 기존 메뉴(`Apps in Toss / 미니앱 변환`)는 그대로 사용 가능합니다.
새로운 `Build & Deploy Window`는 Settings가 통합되어 더 편리합니다.

---

### Q4: WebGLTemplates vs Vite 빌드?

**A**: 최신 버전은 WebGLTemplates (AITTemplate2022)를 사용합니다.
- ✅ Unity 네이티브 WebGL 템플릿 시스템
- ✅ 별도의 빌드 과정 불필요
- ✅ Apps in Toss 브릿지 코드 자동 포함
- ✅ 더 빠른 빌드 속도
- ✅ 안정성 높음

---

### Q5: Git에 커밋해도 되나요?

**A**:
- ✅ 커밋 권장: `AITConfig.asset` (팀원과 공유)
- ❌ 커밋 금지: `ait-build/`, `webgl/` (빌드 결과물)

`.gitignore`에 추가:
```
ait-build/
webgl/
```

---

### Q6: Settings가 저장 안됩니다

**A**: Settings는 자동으로 저장됩니다.
- 저장 위치: `Assets/AppsInToss/Editor/AITConfig.asset`
- 창을 닫을 때 자동 저장
- 값 변경 시 즉시 저장

---

### Q7: 빌드 로그를 어디서 확인하나요?

**A**:
1. `Build & Deploy Window` 하단의 로그 영역
2. Unity Console (Window > General > Console)

---

### Q8: 개발 서버는 어떻게 사용하나요?

**A**:
1. 먼저 "🚀 Build & Package" 또는 "📦 Package Only" 실행
2. "▶️ Start Dev Server" 버튼 클릭
3. 브라우저에서 `http://localhost:5173/index.html` 접속 (또는 설정한 포트)
4. 테스트 완료 후 "⏹️ Stop Dev Server" 버튼으로 중지

**기술**: Python SimpleHTTPServer를 사용하여 정적 파일을 서빙합니다.

**장점**: 별도의 npm 설치 없이 즉시 로컬 테스트 가능합니다.

---

## 추가 리소스

- [Apps in Toss 공식 문서](https://developers-apps-in-toss.toss.im/)
- [Apps in Toss 콘솔](https://apps-in-toss.toss.im/)
- [Unity WebGL 가이드](https://docs.unity3d.com/Manual/webgl.html)
- [PRD 문서](PRD.md) (개발자용)

---

## 문의 및 지원

- 📧 이메일: appsintoss@toss.im
- 📖 문서: https://developers-apps-in-toss.toss.im
- 💬 커뮤니티: https://devtalk-apps-in-toss.toss.im

---

**Happy Game Development! 🎮**
